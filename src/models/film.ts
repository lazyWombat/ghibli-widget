export class Film {
    id: string;
    title: string;
    description: string;
    director: string;
    producer: string;
    // tslint:disable-next-line:variable-name
    release_date: number;
    // tslint:disable-next-line:variable-name
    rt_score: number;
}